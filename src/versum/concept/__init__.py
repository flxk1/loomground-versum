"""The concept layer: mental-model curation over the claim substrate.

`canon` is coordinate-identity curation; `curate` is the claim-to-concept suggester;
`morph` is deterministic key_term morphology (ADR-003); `semantics` persists
claim-grounded compositions and nD context.
"""
