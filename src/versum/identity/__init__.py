"""Source identity: deterministic resolution, URNs, fingerprints, and evidence.

`core` mints the one shared deterministic identity (ADR-URN option A); `urn` derives
source URNs; `fingerprint` builds a source's fixed-shape claim aggregate; `evidence`
is the store-backed evidence facade (K4).
"""
