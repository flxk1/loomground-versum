# Concrete development and CI resolution of the abstract ranges in
# pyproject.toml. Kept out of package metadata so the built distribution stays
# index-clean. Install this before `pip install .` so the ranges are already
# satisfied and pip never needs an index for the sibling packages.
# Immutable release commits: governance v0.8.2 and deontic v0.1.2.
loomground-governance @ git+https://github.com/flxk1/loomground-governance@9e91cf8d4b8624fbf017b7a96ec53c3ce366d31e
loomground-deontic @ git+https://github.com/flxk1/loomground-deontic@6fc6df578a35be58924e8d3700fb992b24d68bbb
pytest==9.1.1
