"""Reading the outside world: claim extraction and registry consumption.

`extract` is the profile-parameterized claim extractor; `consume` reads the KG's
19-column source registry (ADR-URN option B).
"""
